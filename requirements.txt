# AEIOU PoC — dependencies
# Install everything:  pip install -r requirements.txt
#
# The mocked pytest suite passes with just the "core" group below.
# The real-API verification scripts (demo_run.py / eval_*.py) additionally
# need the "real API" group.

# --- core (required for app + mocked pytest) ---
fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.7
sqlalchemy>=2.0
python-dotenv>=1.0
pyyaml>=6.0
python-multipart>=0.0.9
numpy>=1.26
scipy>=1.11
soundfile>=0.12
kiwipiepy>=0.17
httpx>=0.27
pytest>=8.0

# --- real API (only for demo_run.py / eval_*.py) ---
anthropic>=0.40
openai>=1.40

# --- optional heavier audio backend ---
# Enables compute_f0_stats(method="pyin"). F0 default is a pure-numpy
# autocorrelation estimator, so this is NOT required.
# librosa>=0.10
